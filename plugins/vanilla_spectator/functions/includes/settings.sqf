
if (isNil "vanillaspectator_whitelisted_sides") then { vanillaspectator_whitelisted_sides = [] };
if (isNil "vanillaspectator_can_view_AI") then { vanillaspectator_can_view_AI = true };
if (isNil "vanillaspectator_free_camera_available") then { vanillaspectator_free_camera_available = true };
if (isNil "vanillaspectator_third_person_available") then { vanillaspectator_third_person_available = true };
if (isNil "vanillaspectator_show_focus_available") then { vanillaspectator_show_focus_available = true };
if (isNil "vanillaspectator_show_buttons") then { vanillaspectator_show_buttons = true };
if (isNil "vanillaspectator_show_controls") then { vanillaspectator_show_controls = true };
if (isNil "vanillaspectator_show_header") then { vanillaspectator_show_header = true };
if (isNil "vanillaspectator_show_entities") then { vanillaspectator_show_entities = true };